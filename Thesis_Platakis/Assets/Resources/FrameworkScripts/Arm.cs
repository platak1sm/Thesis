using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Arm : MuscleGroup
{
    // Start is called before the first frame update
    void Start()
    {
        name = "Arm";
       // muscles.Add(new Shoulder());
    }

    // Update is called once per frame
    void Update()
    {
       
    }
    
}
